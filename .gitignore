# deps / build
node_modules/
package-lock.json
dist/
build/
.next/
.turbo/
coverage/
*.tsbuildinfo

# local usage store + secrets (must NEVER be committed)
.vibetracker/
supabase/.temp/
*.sqlite
.env
.env.*
!.env.example

# os / editor
.DS_Store
*.log
graphify-out/
cc.json

# local SMA scanner cache (contains machine-absolute paths; regenerate on demand)
.sweetspot/scans/

# Local Netlify folder
.netlify

# maintainer-internal planning docs (kept locally, never shipped)
PLAN.md

# SMARCH deploy guard: per-deploy stamp + lock state (a tracked stamp would dirty the tree it certifies)
.sma/
packages/web/public/deploy-stamp.json
.deploy-guard-test-dirty-*
logs/
